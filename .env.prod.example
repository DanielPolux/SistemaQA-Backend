# Variables para docker-compose.prod.yml (se leen desde ".env" en el
# mismo directorio donde se ejecuta `docker compose`, ver DEPLOY abajo).
# Copiar a ".env" y completar con valores reales/seguros. NO commitear el .env real.

DB_USERNAME=sqa_admin
DB_PASSWORD=cambia_esto_por_una_password_segura
DB_DATABASE=sistema_qa
